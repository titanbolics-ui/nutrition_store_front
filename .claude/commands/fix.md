---
description: Apply reviewer feedback
---
The reviewer returned the following feedback on the last stage:

$ARGUMENTS

Process:
1. Address each point in order. If a point conflicts with the spec or codebase
   reality, flag it instead of guessing.
2. Re-run the stage's acceptance tests.
3. Produce a delta-report: point → what changed → test status.